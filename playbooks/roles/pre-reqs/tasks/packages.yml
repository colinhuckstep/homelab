- name: Install Package Prereqs
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - git
      - python3-full
      - python3-pip
      - python3-venv
      - python3-virtualenv
      - jq
      - sudo
    update_cache: true
